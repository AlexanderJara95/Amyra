@model IEnumerable<Amyra.Models.Producto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a href="@Url.Action("Create")" class="btn btn-custom">Crear Nuevo</a>
</p>
<table class="table">
    <thead style="background-color: #ac8b65;">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PorcentajeDescuento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody style="background-color: #ffdeb9">
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PorcentajeDescuento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-custom">Editar</a>
                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-custom btn-spacing">Detalle</a>
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-custom2 btn-spacing">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>

<style>
    .btn-custom {
        background-color: #f1a64f;
        color: #000000;
        margin-right: 10px;
    }

    .btn-custom2 {
        background-color: #bdbdbd;
        color: #000000;
        margin-right: 10px;
    }
</style>